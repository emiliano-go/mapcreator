services:
  mapcreator:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: mapcreator
    ports:
      - "8080:80"
    volumes:
      - ../exports:/exports
    environment:
      - EXPORT_DIR=/exports
    restart: unless-stopped
